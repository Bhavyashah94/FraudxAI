# Tracking File for Current Active Vertical Slice
# Read this file at the start of any new session or task to immediately anchor context.

current_slice:
  id: "slice-04-synthetic-leak-elimination-emv-primacy-and-intervention-support"
  title: "Synthetic Leak Elimination, EMV Chip Cryptographic Primacy & Attack Script Intervention Support Benchmark"
  status: "verified"  # [planning, in_progress, testing, verified]
  objective: >
    Address the synthetic leak and hard negative criticisms from the Prism peer review:
    1. Eliminate IP Distance Leak: Replace 0.000 km shortcut across all 11 attack playbooks with realistic non-zero network distances (min >= 1.5 km).
    2. Enforce EMV Chip Cryptographic Primacy: Place authentic EMV Contact Chip verification before ML threshold declines, ensuring 0% ISO 59 false declines.
    3. Causal SCM Logarithmic Saturation: Saturate amount ratios via ln(1 + max(0, ratio - 1)) and suppress velocity-distance synergy on verified chip presence.
    4. Attack Script Intervention Support Benchmark: Evaluate TreeSHAP against the attack script's actively intervened features (Precision@K, Recall@K).
    5. Anti-Leak Tripwires: Ensure PR-AUC <= 0.985 for GBDT ensembles and single-feature attribution share <= 70%.

target_spec_files:
  - "spec/01_financial_instruments.yaml"
  - "spec/02_human_personas.yaml"
  - "spec/03_payment_rail_gaps.yaml"
  - "spec/04_adversarial_playbooks.yaml"
  - "spec/05_india_payment_rails.yaml"

acceptance_criteria:
  - zero_synthetic_ip_distance_leak: true # Min IP distance >= 1.5 km, 0 captured by <= 0.5 split
  - emv_chip_primacy_enforced: true # 0% ISO 59 on authentic EMV contact chip, >= 85% approval
  - causal_scm_log_saturation_active: true # ln(1 + ratio) prevents log-odds explosion
  - attack_script_intervention_support_benchmark_added: true # Precision@3 >= 0.70, Recall@3 >= 0.65
  - anti_leak_tripwires_verified: true # PR-AUC realistic, single feature <= 70% share
  - all_63_tests_passed: true # 100% test pass rate in pytest (63/63)
